---
import { Image } from "astro:assets";
interface Props {
  author: string;
  role: string;
  image: ImageMetadata;
}
const props = Astro.props;
---

<article class="splide__slide bg-highlight rounded-2xl p-5">
  <div class="text-h2 text-muted-foreground font-medium">“</div>
  <blockquote class="text-muted-foreground mb-6 text-lg">
    <slot />
  </blockquote>
  <footer class="flex items-center gap-3">
    <Image
      src={props.image}
      alt={props.author}
      width={44}
      height={44}
      class="rounded-lg"
    />
    <div class="space-y-1 text-sm">
      <cite class="font-medium">{props.author}</cite>
      <div class="text-muted-foreground">{props.role}</div>
    </div>
  </footer>
</article>
